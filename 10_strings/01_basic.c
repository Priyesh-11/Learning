#include <stdio.h>

int main() {

    // Declare a string
    // char arr[]; // array of characters
    // char arr[] = {'P','r','i','y','e','s','h','\0'}; // string initialization

    // int i = 0;
//\0 is the null terminator used to indicate the end of a string
    // while (arr[i] != '\0') {
    //     printf("%c", arr[i]);
    //     i++;
    // }    not recommended due to the use of magic numbers
    
    char arr[] = "Priyesh"; // string literal
    // string literal is an array of characters with a null terminator at the end


    return 0;
}